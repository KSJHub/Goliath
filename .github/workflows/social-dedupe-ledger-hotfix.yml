# trigger
name: Social Studio persistent dedupe ledger hotfix
on:
  push:
    branches: [beta]
    paths: ['.github/workflows/social-dedupe-ledger-hotfix.yml']
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo 'workflow trigger active'
