'use strict';
const fs=require('fs');const path=require('path');const auditStore=require('./auditStore');
const root=path.join(auditStore.getRoot(),'message-evidence');
function safe(v){return String(v||'unknown').replace(/[^a-zA-Z0-9_-]/g,'_');}function file(guildId,messageId){return path.join(root,safe(guildId),`${safe(messageId)}.json`);}function read(guildId,messageId){try{return JSON.parse(fs.readFileSync(file(guildId,messageId),'utf8'));}catch{return null;}}function write(guildId,messageId,value){const f=file(guildId,messageId);fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,`${JSON.stringify(value,null,2)}\n`,'utf8');return value;}
function record(snapshot,kind='observed'){if(!snapshot?.guildId||!snapshot?.id)return null;const current=read(snapshot.guildId,snapshot.id)||{guildId:snapshot.guildId,messageId:snapshot.id,firstObservedAt:new Date().toISOString(),revisions:[],deleted:null};const revision={kind,observedAt:new Date().toISOString(),...snapshot};const last=current.revisions.at(-1);if(!last||JSON.stringify({content:last.content,attachments:last.attachments,embeds:last.embeds,stickers:last.stickers})!==JSON.stringify({content:revision.content,attachments:revision.attachments,embeds:revision.embeds,stickers:revision.stickers}))current.revisions.push(revision);current.revisions=current.revisions.slice(-50);current.latest=revision;return write(snapshot.guildId,snapshot.id,current);}
function markDeleted(snapshot,attribution=null){if(!snapshot?.guildId||!snapshot?.id)return null;const current=record(snapshot,'delete-observed')||{};current.deleted={observedAt:new Date().toISOString(),attribution:attribution||null};return write(snapshot.guildId,snapshot.id,current);}
function latest(guildId,messageId){const x=read(guildId,messageId);return x?.latest||x?.revisions?.at?.(-1)||null;}function previous(guildId,messageId){const x=read(guildId,messageId),r=x?.revisions||[];return r.length>1?r[r.length-2]:r[0]||null;}
module.exports={record,markDeleted,read,latest,previous,getRoot:()=>root};
