'use strict';

// Stable public Welcome module. The canonical implementation is shared here
// so Discord events, dashboard routes and panels all execute the same code.
module.exports = require('./welcomeCore');
